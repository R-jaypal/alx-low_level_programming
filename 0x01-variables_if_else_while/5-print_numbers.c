#include <stdio.h>

/**
 * main - prints all single digits of base ten
 *
 * Return: 0 when through
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
