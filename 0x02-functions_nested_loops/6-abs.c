#include "main.h"

/**
 * _abs - function computes absolute value of an integer
 *@a: parameter
 * Return: -a or a;
 */

int _abs(int a)
{
	if (a < 0)
		return (-a);
	else
		return (a);

	return (0);
}
